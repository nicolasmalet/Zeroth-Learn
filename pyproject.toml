[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "zeroth-learn"
version = "0.2.2"
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "cycler"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["zeroth*"]
exclude = ["lab*"]